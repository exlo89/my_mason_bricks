name: feature
description: A brick to create a screen using best practices and bloc as state management!
version: 0.1.0

environment:
  mason: ">=0.1.0-dev.50 <0.1.0"

vars:
  name:
    type: string
    default: Default
    description: The page name
    prompt: What is the page name?
  event:
    type: string
    default: Fetch
    description: The initial bloc event.
    prompt: What is the name of the initial event?
